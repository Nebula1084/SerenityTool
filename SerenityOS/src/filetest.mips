.ORIGIN 0x0
		addi	$sp, $zero, 0x2FFE #initial the stack pointer
		move	$gp, $zero

		li		$v0, 5
		syscall
		
		li		$v0, 4
		li		$a0, 0x61
		li		$a1, 0
		li		$a2, 0
#		syscall
		
exit:	beq $zero, $zero, exit
		.2byte	exit